@page "/register"

<section class="auth-page">
    <header>
        <p class="eyebrow">Register</p>
        <h1>Create your workspace access</h1>
        <p class="lede">
            Hook this page up to Azure Entra ID or Azure AD B2C. After registration, ensure an application-level
            user record exists and associate a Stripe customer ID.
        </p>
    </header>

    <div class="auth-card">
        <p class="auth-copy">
            This placeholder keeps the route alive and demonstrates where the registration call-to-action lives in the UI.
            Replace the button below with the actual OIDC sign-up flow when wiring identity.
        </p>
        <button class="btn primary" disabled>Register with Entra (coming soon)</button>
        <p class="sub-copy">
            Future: redirect to the IdP, then create/update the user profile and initialize free-tier quotas.
        </p>
    </div>
</section>
