@page "/signin"

<section class="auth-page">
    <header>
        <p class="eyebrow">Sign in</p>
        <h1>Access your workspace</h1>
        <p class="lede">
            This is a placeholder for the interactive sign-in flow. Connect to Entra ID / B2C and redirect users back
            to <code>/app</code> with their profile hydrated.
        </p>
    </header>

    <div class="auth-card">
        <p class="auth-copy">
            Wire the button below to kick off the OIDC sign-in flow. Once authenticated, resolve entitlements from the
            database and Stripe subscription state.
        </p>
        <button class="btn ghost" disabled>Sign in with Entra (coming soon)</button>
        <p class="sub-copy">
            Future: add persistent component state so in-flight chat sessions survive reconnects.
        </p>
    </div>
</section>
